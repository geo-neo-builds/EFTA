"""Local entity extraction: spaCy NER + regex for phones/emails/money.

Used by the zero-cost pipeline to populate faceted filters on the site
(people, organizations, locations, dates, dollar amounts, phones, emails).
"""
